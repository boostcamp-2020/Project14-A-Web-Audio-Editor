describe('Sample Test', () => {
  beforeAll(async () => {
    console.log('사전작업');
  });

  test('Sample Test', async () => {
    // given

    // when

    // then
    expect(1).toEqual(1);
  });
});
